# Defaults to this same value if unset — only needed to point at a
# different API instance.
VITE_API_BASE_URL=http://localhost:3001

# Only needed if the API has API_KEY set (see the root .env.example) — must
# match it exactly. Not a real secret once built into this bundle.
VITE_API_KEY=
