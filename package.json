{
  "name": "ai-powered-portfolio",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "start:api": "pnpm --filter @portfolio/api dev",
    "start:web": "pnpm --filter @portfolio/web dev",
    "dev": "concurrently -n api,web -c blue,green \"pnpm start:api\" \"pnpm start:web\"",
    "sync:notion": "node --env-file-if-exists=.env scripts/sync-notion.js"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
